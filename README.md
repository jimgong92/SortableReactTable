# Sortable React Table Component

A react table component that dynamically receives data with React props and can be sorted by column. Utilizes quicksort.

## Development

```
npm install
gulp
npm start
```
